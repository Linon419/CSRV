{
  "name": "backtest-tool",
  "version": "1.0.0",
  "description": "潜力区币回测工具 - Cloudflare部署版",
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB",
    "deploy": "wrangler pages deploy public",
    "d1:create": "wrangler d1 create backtest-db",
    "d1:init": "wrangler d1 execute backtest-db --file=./schema.sql"
  },
  "keywords": [
    "crypto",
    "backtest",
    "trading",
    "cloudflare"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
